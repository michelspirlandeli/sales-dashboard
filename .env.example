# Backend - Spring profile (test | dev | prod)
APP_PROFILE=dev

# Backend - Database (required when APP_PROFILE=dev or prod)
DB_URL=jdbc:postgresql://localhost:5432/dsvendas
DB_USERNAME=postgres
DB_PASSWORD=

# Backend - Production only (overrides DB_URL/USERNAME/PASSWORD)
# DATABASE_URL=

# Frontend
REACT_APP_BACKEND_URL=http://localhost:8090
